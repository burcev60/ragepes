## **Работа со сложными ветками и восстановление коммитов**

### **Использование `git reflog` для восстановления коммитов**

`git reflog` позволяет увидеть все действия с веткой, включая удалённые коммиты.

**Восстановление утерянного коммита:**

`git checkout <commit-hash> `

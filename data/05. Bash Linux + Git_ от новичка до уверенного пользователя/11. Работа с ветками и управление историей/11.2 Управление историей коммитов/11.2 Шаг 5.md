## **Просмотр истории коммитов**

### **`git log`: полная история**

`git log `

Вы увидите список всех коммитов с датой, автором и сообщением.

### **`git log --oneline`: краткая история**

`git log --oneline `

Каждый коммит будет отображён в одну строку с кратким хэшем и сообщением.

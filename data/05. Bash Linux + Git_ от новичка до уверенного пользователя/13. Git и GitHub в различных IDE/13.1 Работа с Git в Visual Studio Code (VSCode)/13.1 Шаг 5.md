### **Слияние веток**

Чтобы объединить ветку `feature-branch` с основной веткой `main`, переключитесь на `main` и выберите **Merge Branch** в меню веток.

**Через терминал:**

`git checkout main git merge feature-branch`

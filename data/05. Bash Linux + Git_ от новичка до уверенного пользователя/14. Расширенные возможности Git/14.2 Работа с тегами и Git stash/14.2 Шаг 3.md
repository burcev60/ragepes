## **Управление тегами**

### **1. Удаление тега:**

Удалить тег из локального репозитория можно командой:

`git tag -d v1.0 `

### **2. Отправка тега в удалённый репозиторий:**

`git push origin v1.0 `

### **3. Удаление тега из удалённого репозитория:**

`git push origin --delete v1.0`

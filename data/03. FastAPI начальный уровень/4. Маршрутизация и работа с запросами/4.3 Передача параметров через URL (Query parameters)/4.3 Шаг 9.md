**Проверка делимости:**  
Создайте маршрут `/divisible`, который принимает параметры запроса `num` и `divisor` и проверяет, делится ли число на делитель без остатка.  
Пример:  
Запрос: `/divisible?num=10&divisor=2`  
Ответ:

`{"is_divisible": true}`

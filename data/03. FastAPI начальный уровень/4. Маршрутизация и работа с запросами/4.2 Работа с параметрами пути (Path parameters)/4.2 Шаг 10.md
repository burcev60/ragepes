**Объединение строк:**  
Создайте маршрут `/concat/{str1}/{str2}`, который принимает две строки через путь и возвращает их объединение через пробел.  
Пример:  
Запрос: `/concat/hello/world`  
Ответ:

`{"result": "hello world"}`

**Удаление задачи (DELETE):**  
Создайте маршрут `/task/{id}` с методом `DELETE`, который удаляет задачу по идентификатору и возвращает сообщение об удалении.  
Пример:

`{"message": "Task 1 deleted."}`

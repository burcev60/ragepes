В фрагменте ниже пропущено имя метода, который *добавляет* или *обновляет* точки в коллекции.

    client = QdrantClient(url="http://localhost:6333")
    client.________(  # ← заполните
        collection_name="articles",
        points=[
            {"id": 42, "vector": vector_42, "payload": {"year": 2024}}
        ]
    )

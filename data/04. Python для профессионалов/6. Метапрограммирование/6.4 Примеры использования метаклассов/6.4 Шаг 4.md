> *«Что из перечисленного можно сделать с помощью метакласса в момент объявления класса?»*

Под «моментом объявления» тут понимаем именно этап `__new__` / `__init__` метакласса, когда он получает `name, bases, namespace` и возвращает готовый объект класса.

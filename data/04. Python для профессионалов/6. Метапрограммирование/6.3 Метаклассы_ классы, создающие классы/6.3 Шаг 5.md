Выберите все варианты кода, в которых метакласс `RegistryMeta` действительно будет автоматически добавлять каждый создаваемый класс в словарь `registry`.

- A.

    registry = {}
    class RegistryMeta(type):
        def __new__(mcs, name, bases, attrs):
            cls = super().__new__(mcs, name, bases, attrs)
            registry[name] = cls
            return cls

   

- B.

    class RegistryMeta:
        def __new__(mcs, name, bases, attrs):
            return attrs

   

- C.

    registry = {}
    class RegistryMeta(type):
        def __init__(cls, name, bases, attrs):
            registry[name] = cls
            super().__init__(name, bases, attrs)

   

- D.

    registry = {}
    def RegistryMeta(name, bases, attrs):
        cls = type(name, bases, attrs)
        registry[name] = cls
        return cls

   

- E.

    class RegistryMeta(type):
        pass

   

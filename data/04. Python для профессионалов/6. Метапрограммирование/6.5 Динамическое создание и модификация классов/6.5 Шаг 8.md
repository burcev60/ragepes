Считайте одну строку, содержащую целое число n. С помощью `type()` динамически создайте класс `Dummy` с методом `get_value`, который в исходном определении возвращает значение n. Затем выполните «монки-пэтчинг» (monkey patching), изменив метод `get_value` так, чтобы он возвращал квадрат числа n вместо n.  
Создайте экземпляр класса `Dummy` и вызовите изменённый метод `get_value`, верните его результат в виде строки.

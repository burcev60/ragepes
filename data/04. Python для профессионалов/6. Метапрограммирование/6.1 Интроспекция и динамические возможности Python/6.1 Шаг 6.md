Считайте одну строку, содержащую имя нового класса. С помощью функции `type` динамически создайте новый класс с данным именем (наследуемый от `object` и без дополнительных атрибутов). Верните строку, равную имени созданного класса (используйте атрибут `__name__`).

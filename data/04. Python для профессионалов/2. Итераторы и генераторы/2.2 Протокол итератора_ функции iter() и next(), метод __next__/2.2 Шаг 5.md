Чем закончится выполнение следующего кода? (Предположим, список `lst` содержит хотя бы один элемент.)

     

    lst = [10, 20, 30]
    it = iter(lst)
    print(next(it))
    print(next(it))
    print(next(it))
    print(next(it))

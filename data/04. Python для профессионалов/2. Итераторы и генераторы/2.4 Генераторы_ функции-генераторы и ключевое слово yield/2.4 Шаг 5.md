Что выведет следующая программа?

     

    def gen():
        for i in range(3):
            yield i
        yield "x"
        yield "y"
    g = gen()
    print(next(g), next(g))
    for x in g:
        print(x, end=" ")

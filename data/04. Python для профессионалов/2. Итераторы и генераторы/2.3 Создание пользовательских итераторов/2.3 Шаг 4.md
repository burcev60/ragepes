Рассмотрим класс

    class Counter:
        def __init__(self, n):
            self.i = 0
            self.n = n
        def __iter__(self):
            return self
        def __next__(self):
            if self.i < self.n:
                result = self.i
                self.i += 1
                return result
            else:
                raise StopIteration

Что произойдет, если создать объект `c = Counter(5)` и выполнить два независимых цикла `for x in c: ...` подряд?

Что выведет следующий код, использующий декоратор вручную:

    def dec(func):
        def wrapper(*args, **kwargs):
            print("До вызова")
            result = func(*args, **kwargs)
            print("После вызова")
            return result
        return wrapper

    def hello(name):
        print(f"Привет, {name}!")

    hello = dec(hello)
    hello("Мир")

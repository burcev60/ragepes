Что произойдет при выполнении следующего кода?

    def make_multiplier(x):
        def multiplier(y):
            return x * y
        return multiplier

    f2 = make_multiplier(2)
    print(f2(5))

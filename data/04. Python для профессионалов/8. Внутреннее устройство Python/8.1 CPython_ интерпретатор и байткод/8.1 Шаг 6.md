Определите функцию `foo(x)` следующим образом:

    def foo(x):
         return x + 1

Используя модуль `dis`, получите последовательность байткод-инструкций этой функции и сформируйте строку, содержащую только имена опкодов, разделённые пробелом.

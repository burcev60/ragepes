Напишите асинхронную функцию `compute_square(n)`, которая принимает целое число `n`, ожидает 0.01 секунды и возвращает квадрат числа `n`.  
В основной программе считайте одно целое число, затем вызовите `compute_square(n)` через `asyncio.run` и верните результат в виде строки.

## 🔹 Задание: Надень бренд-голос на бота

Вам предстоит превратить консольного бота из Модуля 1 в бренд-ассистента магазина с единым стилем ответов с помощью **LangChain**, создать и продемонстрировать работу инструмента, который автоматически оценивает стиль ответов LLM и формирует отчёт (в формате JSON) с найденными нарушениями и оценками.

Мы разработаем скрипт `style_eval.py`, который либо принимает на вход набор вопросов, отвечает на них в заданном стиле и с определённой ролью, а потом проверяет эти ответы на соответствие указанной роли и стилю. Результат работы скрипта – файл `style_eval.json`, содержащий подробности по каждому проверенному ответу и суммарную статистику.

### 🔸 **Основные составляющие скрипта**

1. **Набор тестовых запросов** для проверки. Например, можно взять 10 типичных пользовательских вопросов и сгенерировать на них ответы с помощью LLM.

2. **Голос бренда** – Шаблон промпта на основе YAML-гайда роли ассистента и тона бренда, который передаётся в системный промпт.

3. **Few-shot** примеры, автоматически подмешиваемые в промпт.

4. **Автооценка стиля** (LLM-оценка + простые условия) – можно использовать GPT как высокоточную модель оценки стиля (например, GPT-4o — если важно снизить стоимость, или GPT-5 — если требуется максимальная точность). Вы должны проверять ответ по критериям соответствия с п.2.

5. **Формирование отчёта** – собираем всё в структуру данных и сохраняем в JSON.

 

### 🔸 Архитектура и правила стиля

**Идея:** все правила — в **данных**, а не в коде.

- `style_guide.yaml` хранит голос бренда: приветствия, длину, лексику-табу, формат ответа.

- `few_shots.jsonl` — 3–5 коротких диалогов «как надо».

- Промпт собирается из: *system (правила) + few-shot + текущий контекст (FAQ/статусы) + запрос пользователя*.

- Вывод — **строго структурированный** (удобно валидировать и рендерить в UI).

 

🔸 **Мини-гайд по стилю (пример YAML)**

    brand: Shoply
    tone:
      persona: "вежливый, деловой, дружелюбный без панибратства"
      sentences_max: 3
      bullets: true
      avoid:
        - "эмодзи"
        - "восклицания!!!"
        - "канцелярит: согласно вышеизложенному, осуществите"
      must_include:
        - "простые формулировки"
        - "варианты решения, если есть"
    fallback:
      no_data: "У меня нет точной информации. Могу подключить оператора или оформить запрос."
    format:
      fields:
        answer: "краткий ответ"
        tone: "контроль: совпадает ли тон (да/нет) + одна фраза почему"
        actions: "список следующих шагов для клиента (0–3 пункта)"

 

### 🔸 Пример содержания JSON отчета

После запуска `style_eval.py` мы ожидаем получить файл `style_eval.json` (отформатировано для читабельности):

    {
      "cases": [
        {
          "user": "Здравствуйте, у меня проблема с заказом.",
          "assistant": "Привет! Давай посмотрим, что случилось с твоим заказом.",
          "rule_issues": {
            "second_person_informal": true,
            "slang_detected": false
          },
          "llm_evaluation": "1) Тон неофициальный (обращение на \"ты\"), 2) сленга нет, 3) на \"Вы\" не обращается."
        },
        {
          "user": "Добрый день. Не могли бы вы подсказать статус заявки 324?",
          "assistant": "Добрый день! С удовольствием помогу вам узнать статус вашей заявки №324.",
          "rule_issues": {
            "second_person_informal": false,
            "slang_detected": false
          },
          "llm_evaluation": "1) Тон вежливый, 2) сленга нет, 3) обращение корректное на \"вы\" (хотя написано с маленькой буквы, это допустимо в тексте)."
        },
        {
          "user": "Чё по доставке, где мой товар?",
          "assistant": "Добрый день. Пожалуйста, уточните номер вашего заказа, чтобы я мог проверить информацию.",
          "rule_issues": {
            "second_person_informal": false,
            "slang_detected": false
          },
          "llm_evaluation": "1) Тон вежливый, официальный, 2) сленга нет (ассистент сам не использует сленг, хотя пользователь использовал), 3) обращение на \"вы\"."
        }
      ],
      "summary": {
        "total_cases": 3,
        "violations": {
          "second_person_informal": 1,
          "slang_detected": 0
        }
      }
    }

В данном отчёте мы видим, что в первом ответе ассистент нарушил стиль (обратился с "Привет", на "ты", что не соответствует официальному тону). Это отражено и правилом (`second_person_informal = true`), и LLM-оценкой (которая заметила обращение на "ты").

Во втором и третьем случаях нарушений нет. Сводная статистика summary показывает, что из 3 ответов 1 содержит обращение на "ты", сленг не обнаружен ни в одном. Этот отчёт `style_eval.json` поможет нам понять, где модель отклоняется от гайдлайнов.

 

### 🔸 **Как использовать отчёт**

На основе подобного отчёта команда разработчиков или контент-редакторов может решить, что нужно скорректировать.

**В нашем примере видно:** ассистент в неформальной речи пользователя тоже сначала ответил неформально («Привет!») – возможно, систему надо настроить, чтобы **всегда** сохранять официальный тон независимо от тона пользователя. То есть, внести поправку в system prompt: `Никогда не используешь разговорных сокращений, даже если пользователь их употребляет`*.* Или добавить в **few-shot** набор пример, где пользователь грубит, а ассистент отвечает вежливо. Повторный запуск оценки покажет, исправилась ли ситуация.

- Если объём ответов большой, можно генерировать отчёт выборочно, или использовать более дешёвые модели для части критериев. Например, детектировать специфические слова, можно и без GPT.

- Авто-отчёт не заменяет финальной ручной проверки наиболее критичных случаев – он служит фильтром, сигнализирующим о проблемах.

- Включайте в отчёт не только нарушения, но и позитивные моменты: например, процент ответов, где полностью соблюдены все критерии. Это важно для метрик качества обслуживания.

- Используйте версионирование: если вы обновили промпт или модель, снимите новый отчёт `style_eval.json` и сравните с предыдущим – улучшился ли результат или может наоборот, появились новые проблемы.

 

### 🔸 **Структура репозитория**

    ecom-bot/
      app_lc.py
      src/
        brand_chain.py
        style_eval.py
      data/
        style_guide.yaml
        few_shots.jsonl
        eval_prompts.txt
        faq.json          # из М1 (или обновите)
        orders.json       # из М1
      reports/            # автогенерация
      .env.example
      requirements.txt
      README.md

 

### 🔸 **data/few_shots.jsonl (пример)**

    {"user":"Как оформить возврат?","assistant":"Вернуть можно в течение 14 дней.\n- Заполните форму в личном кабинете\n- Упакуйте товар и приложите чек\n- Отправьте посылку любой службой"}
    {"user":"Можно ли изменить адрес?","assistant":"Если заказ не отгружен — изменим.\n- Напишите номер заказа\n- Укажите новый адрес\n- Я проверю доступность"}
    {"user":"Какие способы оплаты принимаете?","assistant":"Доступны карты и СБП.\n- Visa/Mastercard\n- СБП\n- Подарочные сертификаты Shoply"}

 

### 🔸 **data/eval_prompts.txt (пример 10 строк)**

    Сколько идёт доставка?
    Можно ускорить доставку?
    Где ввести промокод?
    У меня сломан товар, что делать?
    Могу ли я объединить два заказа?
    Поменяйте адрес на Пушкина 10
    Где находится ваш пункт самовывоза?
    Сколько дней на возврат?
    Заказ 55555 — что со статусом?
    Вы принимаете криптовалюту?

 

### 🔸 Пример скрипта Автооценки стиля `src/style_eval.py` (score ≥ 80/100)

    import os, json, pathlib, re, statistics
    from typing import List
    from dotenv import load_dotenv
    from pydantic import BaseModel, Field
    from langchain_openai import ChatOpenAI
    from langchain_core.prompts import ChatPromptTemplate
    from brand_chain import ask, STYLE, BASE

    load_dotenv(BASE / ".env", override=True)
    REPORTS = BASE / "reports"
    REPORTS.mkdir(exist_ok=True)

    # Простые проверки до LLM
    def rule_checks(text: str) -> int:
        score = 100
        # 1) Без эмодзи
        if re.search(r"[\U0001F300-\U0001FAFF]", text):
            score -= 20
        # 2) Без крика!!!
        if "!!!" in text:
            score -= 10
        # 3) Длина
        if len(text) > 600:
            score -= 10
        return max(score, 0)

    # LLM-оценка
    class Grade(BaseModel):
        score: int = Field(..., ge=0, le=100)
        notes: str

    LLM = ChatOpenAI(model=os.getenv("OPENAI_MODEL","gpt-4o-mini"), temperature=0)

    GRADE_PROMPT = ChatPromptTemplate.from_messages([
        ("system", f"Ты — строгий ревьюер соответствия голосу бренда {STYLE['brand']}"),
        ("system", f"Тон: {STYLE['tone']['persona']}. Избегай: {', '.join(STYLE['tone']['avoid'])}. "
                   f"Обязательно: {', '.join(STYLE['tone']['must_include'])}."),
        ("human", "Ответ ассистента:\n{answer}\n\nДай целочисленный score 0..100 и краткие заметки почему.")
    ])

    def llm_grade(text: str) -> Grade:
        parser = LLM.with_structured_output(Grade)
        return (GRADE_PROMPT | parser).invoke({"answer": text})

    def eval_batch(prompts: List[str]) -> dict:
        results = []
        for p in prompts:
            reply = ask(p)
            rule = rule_checks(reply.answer)
            g = llm_grade(reply.answer)
            final = int(0.4 * rule + 0.6 * g.score)
            results.append({
                "prompt": p,
                "answer": reply.answer,
                "actions": reply.actions,
                "tone_model": reply.tone,
                "rule_score": rule,
                "llm_score": g.score,
                "final": final,
                "notes": g.notes
            })
        mean_final = round(statistics.mean(r["final"] for r in results), 2)
        out = {"mean_final": mean_final, "items": results}
        (REPORTS / "style_eval.json").write_text(json.dumps(out, ensure_ascii=False, indent=2), encoding="utf-8")
        return out

    if __name__ == "__main__":
        eval_prompts = (BASE / "data/eval_prompts.txt").read_text(encoding="utf-8").strip().splitlines()
        report = eval_batch(eval_prompts)
        print("Средний балл:", report["mean_final"])
        print("Отчёт:", REPORTS / "style_eval.json")

**Запуск**

    pip install -r requirements.txt
    cp .env.example .env  # впишите OPENAI_API_KEY и (опц.) OPENAI_MODEL
    python src/brand_chain.py   # демо цепочки
    python app_lc.py            # чат-бот в стиле бренда
    python src/style_eval.py    # автооценка (сохранит reports/style_eval.json)

 

### 🔸 Что сдавать на GitHub

**Содержимое репозитория**

- Код: `app_lc.py`, `src/brand_chain.py`, `src/style_eval.py`.

- Данные: `data/style_guide.yaml`, `data/few_shots.jsonl`, `data/eval_prompts.txt`, `data/faq.json`, `data/orders.json`.

- Конфиги: `.env.example`, `requirements.txt`.

- Отчёт: `reports/style_eval.json`.

- `README.md` с командами запуска и описанием.

**Проверка преподавателем**

1. Клонируем репо, ставим зависимости, запускаем `style_eval.py`.

2. `mean_final ≥ 80` — зачёт по тону.

3. Быстрый ручной прогон чата: вопрос из FAQ + `/order 98765` → вменяемые брендовые ответы, без фантазий.

4. Проверяем, что структурированный ответ содержит `answer`, `tone`, `actions` (не пустые без причины).

 

### 🔸 Структурированный ответ ассистента: `answer` / `tone` / `actions`

**Зачем поле `tone`?** Это служебная мета-информация для разработчика: она позволяет автоматически проверять «попадание в голос бренда», перегенерировать неудачные ответы и собирать метрики. Клиенту показывается только `answer` (и, при желании, `actions`), а `tone` остаётся в логах/оценке.

#### Примеры

**1) Попадание в тон**

    {
      "answer": "Промокод вводится в корзине перед оплатой.",
      "tone": "да — кратко, без эмоций, есть конкретика.",
      "actions": [
        "Откройте корзину",
        "Нажмите «Ввести промокод»",
        "Введите код и подтвердите"
      ]
    }

**2) Не попали — повод переформулировать**

    {
      "answer": "Конечнооо! Просто влепите код куда-нибудь в оплате 😁",
      "tone": "нет — разговорно, эмодзи и восклицания, нарушен гайд.",
      "actions": [
        "Откройте корзину",
        "Нажмите «Ввести промокод»",
        "Введите код и подтвердите"
      ]
    }

 

### 🔸 Критерии зачёта (чек-лист)

- Репозиторий на GitHub с читаемым README и инструкцией запуска.

- Конфиги: `.env.example`, `requirements.txt`, `data/style_guide.yaml`, `data/few_shots.jsonl`.

- Реализован **LangChain-chain** с `ChatOpenAI` и `ChatPromptTemplate`.

- Ответы парсятся в Pydantic-модель (структурированный вывод) **или** через `with_structured_output`.

- Скрипт `src/style_eval.py` даёт **средний score ≥ 80/100** на 10 тестовых промптах и сохраняет отчёт.

- Бот «не выдумывает» факты вне FAQ/заказов; при отсутствии данных — корректно отказывает.

- Локальный запуск командой из README проходит без ошибок.
